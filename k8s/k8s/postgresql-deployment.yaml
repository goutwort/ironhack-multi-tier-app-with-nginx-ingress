apiVersion: apps/v1
kind: Deployment
metadata:
  name: po-lin-postgresql
  namespace: po-lin-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: po-lin-postgresql
  template:
    metadata:
      labels:
        app: po-lin-postgresql
    spec:
      containers:
      - name: redis-container
        image: po-lin/lab-postgresql:1.0
        ports:
        - containerPort: 3000
          protocol: TCP
